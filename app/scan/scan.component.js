// Register `atm` component, along with its associated controller and template
angular.
  module('atmApp').
  component('scan', {
    templateUrl: 'scan/scan.template.html',
    controller: ['$http', function ScanController($http) {
        
      }
    ]
  });